using System;

namespace Nums1To10
{
    class Program
    {
        static void Main(string[] args)
        {
            for (int numbers = 1; numbers <= 10; numbers++)
            Console.WriteLine(numbers);
            
        }
    }
}
